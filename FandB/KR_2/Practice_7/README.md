# Practice 7

Сервер на Node.js (Express) с:

- регистрацией и входом (bcrypt-хеширование пароля)
- CRUD для сущности "товар"

## Запуск

```bash
cd KR_2/Practice_7/server
npm i
npm run start
```

Сервер по умолчанию слушает `http://localhost:3000`.

