services:
  mongo:
    image: mongo:7
    ports:
      # 27018 на хосте — если 27017 занят локальным MongoDB
      - "27018:27017"
    volumes:
      - mongodata_pr20:/data/db

volumes:
  mongodata_pr20:

